# apiVersion: apps/v1
# kind: Deployment
# metadata:
#   name: frontend-deployment
#   namespace: drivecloud
#   labels:
#     app: frontend
# spec:
#   replicas: 1
#   selector:
#     matchLabels:
#       app: frontend
#   template:
#     metadata:
#       labels:
#         app: frontend
#     spec:
#       containers:
#       - name: frontend
#         image: YOUR_DOCKERHUB_USERNAME/drivecloud-frontend:latest   # ← Change this
#         imagePullPolicy: Always
#         ports:
#         - containerPort: 80
#         resources:
#           requests:
#             memory: "64Mi"
#             cpu: "50m"
#           limits:
#             memory: "128Mi"
#             cpu: "200m"
#         livenessProbe:
#           httpGet:
#             path: /
#             port: 80
#           initialDelaySeconds: 10
#           periodSeconds: 30
#         readinessProbe:
#           httpGet:
#             path: /
#             port: 80
#           initialDelaySeconds: 5
#           periodSeconds: 10
# ---
# apiVersion: v1
# kind: Service
# metadata:
#   name: frontend-service
#   namespace: drivecloud
#   labels:
#     app: frontend
# spec:
#   selector:
#     app: frontend
#   ports:
#   - protocol: TCP
#     port: 80
#     targetPort: 80
#     nodePort: 30080
#   type: NodePort


apiVersion: apps/v1
kind: Deployment

metadata:
name: frontend-deployment
namespace: drivecloud

spec:
replicas: 1

selector:
matchLabels:
app: frontend

template:
metadata:
labels:
app: frontend

spec:
  containers:
    - name: frontend

      image: kundan333/drivecloud-frontend:latest

      imagePullPolicy: Always

      ports:
        - containerPort: 80

      readinessProbe:
        httpGet:
          path: /
          port: 80
        initialDelaySeconds: 10
        periodSeconds: 10

      livenessProbe:
        httpGet:
          path: /
          port: 80
        initialDelaySeconds: 20
        periodSeconds: 15


---

apiVersion: v1
kind: Service

metadata:
name: frontend-service
namespace: drivecloud

spec:
selector:
app: frontend

ports:
- port: 80
targetPort: 80

type: ClusterIP

