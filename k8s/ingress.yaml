apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: drivecloud-ingress
  namespace: drivecloud
  annotations:
    kubernetes.io/ingress.class: traefik
spec:
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: frontend-service
            port:
              number: 80
